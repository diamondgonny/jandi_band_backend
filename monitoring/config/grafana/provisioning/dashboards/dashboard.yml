# Grafana 대시보드 자동 설정
# 미리 정의된 대시보드를 자동으로 로딩합니다.

apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards 